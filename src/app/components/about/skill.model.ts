export interface Skill {
    name: string;
    rating: number; // This can be a number representing a rating out of 5, for example
  }

  export interface Pfskills {
    name: string;
        rating: number;
  }